<div class="container" style="margin-top:50px">
  <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    
    <div class="field">
      <p><%= f.label :아이디 %><em>아이디는 한글,영문 모두 가능합니다.</em><br />
      <%= f.text_field :username, autofocus: true %></p>
    </div>
    
    <div class="field">
      <p><%= f.label :이름 %><br />
      <%= f.text_field :realname %></p>
    </div>
    
    <div class="field">
      <%= f.label :이메일 %>&nbsp;&nbsp;&nbsp;<em>이메일은 비밀번호 재발급 시 사용됩니다</em><br />
      <%= f.email_field :email %>
    </div>
  
  
    <div class="field">
      <%= f.label :비밀번호 %>
      <% if @minimum_password_length %>
      <em>최소 <%= @minimum_password_length %>자 입니다.</em>
      <% end %><br />
      <%= f.password_field :password, autocomplete: "off" %>
    </div>
  
    <div class="field">
      <%= f.label :비밀번호_확인 %><br />
      <%= f.password_field :password_confirmation, autocomplete: "off" %>
    </div>
  
    <div class="actions">
      <a class="waves-effect waves-light btn deep-orange lighten-2"><%= f.submit "가입하기" %></a>
    </div>
    <div class="field">
      <br>
      <p style="font-style:italic;">*오픈채팅방 > 슥마켓(SGmarket)으로 오셔서 아이디와 학생증을 보내주시면 관리자가 가입 승인을 해드립니다!</p>
      <p style="font-style:italic;">*오픈 채팅방은 1:1 대화로 진행되며 인증 이후 내용은 삭제됩니다</p>
    </div>
  <% end %>
  
  
</div>
