<div class="container" style="margin-top:50px">
  <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    
    <div class="field">
      <p><%= f.label :아이디 %><em>아이디는 한글,영문 모두 가능합니다.</em><br />
      <%= f.text_field :username, autofocus: true %></p>
    </div>
    
    <div class="field">
      <p><%= f.label :이름 %><br />
      <%= f.text_field :realname %></p>
    </div>
    
  
    <div class="field">
      <%= f.label :비밀번호 %>
      <% if @minimum_password_length %>
      <em>최소 <%= @minimum_password_length %>자 입니다.</em>
      <% end %><br />
      <%= f.password_field :password, autocomplete: "off" %>
    </div>
  
    <div class="field">
      <%= f.label :비밀번호_확인 %><br />
      <%= f.password_field :password_confirmation, autocomplete: "off" %>
    </div>
  
    <div class="actions">
      <a class="waves-effect waves-light btn deep-orange lighten-2"><%= f.submit "가입하기" %></a>
    </div>
    <div class="field">
      <br>
      <p style="font-style:italic;">*가입하기를 누르시면 서강대 웹메일로 인증메일이 발송됩니다!</p><p style="font-style:italic;">*인증은 하루 정도의 소요기간이 필요합니다</p>
    </div>
  <% end %>
  
  
</div>
